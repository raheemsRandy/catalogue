name: Hello world Action
on: [push]

jobs: #pipeline
  common: #pipeline1
    uses: /raheemdevops-actions/reusable-workflows/.github/workflows/common.yaml@main
    with:
      project: roboshop
      component: catalogue
      app_type: nodejs